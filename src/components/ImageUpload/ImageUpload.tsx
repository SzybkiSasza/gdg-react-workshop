import * as React from 'react';

class ImageUpload extends React.Component<any> {
  public render(): JSX.Element {
    return <div className={this.props.className}>
      I am image upload dialog
    </div>
  }
}

export default ImageUpload;
